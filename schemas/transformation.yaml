    Transformation:
      description: >-
        Apply one or more transformations to a clip. Transformations alter the visual
        properties of a clip and can be combined to create new shapes and effects.
      type: object
      properties:
        rotate: 
          $ref: "./rotatetransformation.yaml#/RotateTransformation"
        skew:
          $ref: "./skewtransformation.yaml#/SkewTransformation"
        flip:
          $ref: "./fliptransformation.yaml#/FlipTransformation"
