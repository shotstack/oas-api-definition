    SourceListResponse:
      description: >-
        A list of all ingested source files fetched or uploaded to a users account.
      properties:
        data:
          description: An array of ingested source files.
          type: array
          items:
            $ref: "./sourceresponsedata.yaml#/SourceResponseData"
      required:
        - data
      type: object
