    GenerationModelListResponse:
      description: The generation models available to this account.
      properties:
        models:
          description: >-
            The available models. Filtered by the `type` and `status` query parameters
            when supplied.
          type: array
          items:
            $ref: "../generationmodel.yaml#/GenerationModel"
      required:
        - models
      type: object
